export const API_URL = 'http://localhost:8081';

export const GET_CARS = `${API_URL}/car`;
export const REGISTER_CAR = `${API_URL}/car`;
export const DELETE_CAR = `${API_URL}/car`;
export const ADD_PROXY = `${API_URL}/proxy`;
export const SALE = `${API_URL}/contract`;
export const STATE_DUTY = `${API_URL}/duty`;
